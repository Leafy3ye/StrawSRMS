# 会员结算功能测试指南

## 功能概述

会员结算功能现在支持：
1. **余额不足提示**：当会员余额不足时，显示具体的余额和需要支付的金额
2. **会员折扣**：根据会员等级自动应用折扣率
3. **优化的结算成功界面**：显示详细的金额信息和折扣详情

## 测试步骤

### 1. 准备测试数据

#### 设置会员等级
1. 进入"会员政策"页面
2. 添加不同等级的会员，例如：
   - 普通会员：1.0（无折扣）
   - 银卡会员：0.95（9.5折）
   - 金卡会员：0.9（9折）
   - 钻石会员：0.85（8.5折）

#### 创建测试会员
1. 进入"会员管理"页面
2. 添加测试会员：
   - 会员A：余额充足（如¥200），等级为金卡会员
   - 会员B：余额不足（如¥10），等级为普通会员

### 2. 测试余额不足提示

1. 进入任意桌位详情页面
2. 添加一些菜品到订单（总金额超过¥10）
3. 选择"会员结算"
4. 输入会员B的手机号
5. 点击"结算"按钮
6. **预期结果**：显示红色错误提示"会员余额不足！当前余额：¥10.00，需要支付：¥XX.XX"

### 3. 测试会员折扣结算

1. 进入任意桌位详情页面
2. 添加菜品到订单（如总金额¥100）
3. 选择"会员结算"
4. 输入会员A的手机号（金卡会员，9折）
5. 点击"结算"按钮
6. **预期结果**：显示结算成功弹窗，包含：
   - 会员信息（姓名、手机号、等级）
   - 消费总额：¥100.00
   - 会员折扣：9折 (-¥10.00)
   - 实际付款：¥90.00
   - 剩余余额：¥110.00

### 4. 测试普通会员结算

1. 创建一个普通会员（余额充足）
2. 进行会员结算
3. **预期结果**：不显示折扣信息，按原价结算

## 界面优化内容

### 结算成功弹窗特性
- ✅ 居中显示，美观的成功图标
- ✅ 清晰的会员信息展示
- ✅ 详细的金额明细：
  - 消费总额（原价）
  - 会员折扣（如果有）
  - 实际付款（折扣后）
  - 剩余余额
- ✅ 响应式设计，适配不同屏幕
- ✅ 不可通过ESC或点击遮罩关闭，必须点击确定

### 错误提示优化
- ✅ 余额不足时显示具体金额信息
- ✅ 清晰的错误原因说明
- ✅ 红色警告样式，引起用户注意

## 技术实现要点

### 前端改进
1. **折扣计算**：前端获取会员等级信息，计算折扣
2. **余额验证**：结算前检查会员余额是否足够
3. **美观弹窗**：使用自定义弹窗替代简单的alert
4. **错误处理**：完善的错误捕获和用户提示

### 后端改进
1. **异常处理**：MemberController正确返回错误信息
2. **余额验证**：MemberService中的余额检查逻辑
3. **折扣支持**：支持按折扣后金额扣除余额

### 数据流程
1. 用户选择会员结算 → 获取会员信息
2. 获取会员等级 → 计算折扣
3. 验证余额是否足够 → 显示错误或继续
4. 调用结算接口 → 扣除折扣后金额
5. 显示结算成功弹窗 → 展示详细信息

## 常见问题排查

### 问题1：折扣计算不正确
- 检查会员等级设置是否正确
- 确认会员的memberLevel字段与等级名称匹配

### 问题2：余额不足提示不显示
- 检查前端的余额验证逻辑
- 确认会员数据加载正确

### 问题3：结算成功弹窗不显示
- 检查showMemberSettlementSuccess状态
- 确认settlementResult数据设置正确

### 问题4：后端扣除余额失败
- 检查MemberController的异常处理
- 确认DeductRequestDTO数据格式正确
