# 菜品图片更新功能测试指南

## 测试步骤

### 1. 准备测试环境
- 确保后端服务器运行在 http://localhost:8080
- 确保前端服务器运行在 http://localhost:5173
- 登录系统并进入菜品管理页面

### 2. 测试新增菜品图片
1. 点击"添加菜品"按钮
2. 填写菜品名称和价格
3. 点击图片上传区域，选择一张图片
4. 观察是否显示"图片上传成功"提示
5. 点击"添加"按钮保存菜品
6. 检查菜品列表中是否显示了上传的图片

### 3. 测试编辑菜品图片
1. 在菜品列表中点击某个菜品的"编辑"按钮
2. 在弹出的编辑对话框中，点击图片区域
3. 选择一张新的图片进行上传
4. 观察是否显示"菜品图片更新成功"提示
5. 关闭编辑对话框
6. 检查菜品列表中的图片是否已更新

### 4. 验证图片持久化
1. 刷新页面
2. 检查菜品图片是否仍然显示正确
3. 重新编辑菜品，检查图片是否正确显示在编辑表单中

## 预期结果

### 成功情况
- ✅ 图片上传后立即显示在表单中
- ✅ 编辑模式下上传图片后立即保存到数据库
- ✅ 菜品列表中的图片立即更新
- ✅ 刷新页面后图片仍然正确显示
- ✅ 控制台显示保存日志信息

### 可能的问题
- ❌ 图片上传成功但列表中不显示
- ❌ 编辑模式下图片不保存
- ❌ 刷新页面后图片丢失
- ❌ 控制台出现错误信息

## 调试信息

### 前端调试
打开浏览器开发者工具，查看：
1. Console 标签页中的日志信息
2. Network 标签页中的API请求
3. 检查图片上传和菜品更新的请求是否成功

### 后端调试
查看后端控制台输出：
1. 文件上传请求日志
2. 菜品更新请求日志
3. 数据库操作日志

## 修复内容总结

### 前端修复
1. 修复了图片上传成功回调，在编辑模式下立即保存
2. 添加了缓存破坏参数避免图片缓存问题
3. 添加了调试日志帮助排查问题
4. 优化了图片显示逻辑

### 后端修复
1. 在 DishService.updateDish 方法中添加了 imageUrl 字段更新
2. 为 DishController 添加了 @CrossOrigin 注解
3. 确保文件上传配置正确

### 配置修复
1. 文件上传路径配置
2. 静态资源访问配置
3. CORS 跨域配置
